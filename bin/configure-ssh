#!/usr/bin/env python3
"""
OOS SSH Configuration Script
Automates SSH setup for RelayQ architecture using .env variables
"""

import sys
import os
from pathlib import Path

# Add src to path
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))

# Import the configuration function
exec(Path(__file__).parent.parent / "SSH_SETUP_EXPLAINED.md").read_text())